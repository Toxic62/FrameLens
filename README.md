# FrameLens

FrameLens is a desktop tool for viewing, inspecting, and editing Minecraft structure files.

It loads data from a selected Minecraft instance, allowing structures to be rendered with vanilla and modded blocks instead of relying on a fixed built-in block list. FrameLens aims to make it easier to inspect structure contents, edit blocks, review entities and block entities, and work with modded structure files outside the game.
